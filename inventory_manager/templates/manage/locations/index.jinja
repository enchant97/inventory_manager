{% extends "/base.jinja" %}
{% set title="Management - Locations" %}
{% block main %}
<nav>
    <ul>
        <li><a href="{{ url_for('manage.get_index') }}">Back</a></li>
        <li><a href="{{ url_for('manage.get_locations_new') }}">New Location</a></li>
    </ul>
</nav>

<table>
    <caption>Total Counts</caption>
    <thead>
        <tr>
            <th>Roots Count</th>
            <th>Total Count</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{ roots_count }}</td>
            <td>{{ total_count }}</td>
        </tr>
    </tbody>
</table>
{% endblock main %}
